{
  "name": "personal-finance-visualiser",
  "version": "4.2.0",
  "private": true,
  "scripts": {
    "vercel-build": "cd frontend && npm install && npm run build",
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev",
    "start": "cd frontend && npm run preview"
  },
  "devDependencies": {
    "vite": "^5.1.4"
  }
}